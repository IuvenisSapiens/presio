{
  "name": "presio",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && npm start",
    "start:local": "cd server && cross-env PRESIO_MODE=local npm start",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "cross-env": "^10.1.0",
    "tsx": "^4.23.1"
  }
}
